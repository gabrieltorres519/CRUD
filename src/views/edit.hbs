<h1>Pag edit</h1>

<form action="/edit/{{task._id}}" method="POST">
<div>
    <label for="title">Titulo</label>
    <input type="text" name="title" placeholder="Ingresa un titulo" value="{{task.title}}" >
</div>
<div>
    {{!-- Textarea hanclebars solo podrá pintar si se encuentra en el campo y no la descrición --}}
    <label for="description">Descripcion</label>
    <textarea name="description" placeholder="Inserta una descripcion" >{{task.description}}</textarea>
</div>
<button>Guardar</button>
    
</form>